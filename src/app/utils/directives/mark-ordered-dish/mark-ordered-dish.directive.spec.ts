import { MarkOrderedDishDirective } from './mark-ordered-dish.directive';

describe('MarkOrderedDishDirective', () => {
  it('should create an instance', () => {
    const directive = new MarkOrderedDishDirective();
    expect(directive).toBeTruthy();
  });
});
