import { CardActionBarAnimationDirective } from './card-action-bar-animation.directive';

describe('CardActionBarAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new CardActionBarAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
